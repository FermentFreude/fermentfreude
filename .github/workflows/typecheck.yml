name: TypeScript Check

on:
  # Run on every branch push so feature branches (e.g. retouch-*) show check status on commits.
  push:
    branches:
      - '**'
  pull_request:
    branches: [staging, main]
  # Lets you run the check from: Actions → TypeScript Check → Run workflow → pick a branch.
  workflow_dispatch:

jobs:
  typecheck:
    name: Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Payload types
        run: pnpm generate:types
        env:
          DATABASE_URI: ${{ secrets.DATABASE_URI || 'mongodb://localhost:27017/fermentfreude-ci' }}
          PAYLOAD_SECRET: ci-placeholder-secret-min-32-characters-long

      - name: TypeScript check
        run: npx tsc --noEmit
