# Memory

One file per in-progress, multi-session feature: `<feature-slug>.md`.

Written and read by `/project:remember save` / `/project:remember restore` — see [`.claude/commands/remember.md`](../commands/remember.md).

This is scratch handoff state, not documentation. Once a feature ships, its file should be deleted — anything worth keeping long-term belongs in `docs/` instead.
